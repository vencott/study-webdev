# 객체 쓰기와 읽기

```
var coworkers = { // 객체 리터럴로 객체 생성
	"programmer" = "egoing",
	"designer" = "leezche"
};

document.write(coworkers.programmer); 객체 읽기
document.write(coworkers.designer);

cowerkers.bookkeeper = "duru" // 객체 쓰기
document.write(coworkers.bookkeeper);

cowerkers[data scientist] = "taeho";
document.write(coworkers.[data scientist])
```